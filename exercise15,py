# Create a function called add_one that increases the value of x by 1.
# Create a function called add_two that takes x as an argument and returns the value of x plus 2.
def add_one(x):
    x += 1
    return x

def add_two(x):
    return x + 2
